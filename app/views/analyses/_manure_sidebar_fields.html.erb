<span class="-js-input-wrapper">
  <%= f.hidden_field :category, value: Category::MANURE %>
  <%= f.number_field :amount, value: f.object.amount || 0, min: 0 %>
  <%= f.select :addition_type, select_options(Analysis::MANURE_TYPES, f.object.addition_type) %>
  <span class="options-wrapper">
    <button class="btn-reset -js-reset-analysis is-hidden" data-previous-value="<%= f.object.amount %>">
      <svg class="c-icon -small">
        <use xlink:href="#icon-reset"></use>
      </svg>
    </button>
    <span>
      <%= f.hidden_field :_destroy %>
      <button class="-js-remove_fields">
        <svg class="c-icon -small">
          <use xlink:href="#icon-delete"></use>
        </svg>
      </button>
    </span>
  </span>
</span>
